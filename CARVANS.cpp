#include<bits/stdc++.h>
using namespace std;
int main()
{ios_base::sync_with_stdio(false);
    cin.tie(NULL);
	int t;
	cin>>t;
	for(int i=0;i<t;i++)
	{
		int n,input;
		cin>>n;
		vector<>a(n);
		for(int i=0;i<n;i++)
		{cin>>input;
	     a.push_back(input);
		}
		